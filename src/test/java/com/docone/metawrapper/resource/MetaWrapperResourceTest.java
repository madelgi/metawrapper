package com.docone.metawrapper.resource;

import com.docone.metawrapper.model.metamap.MMResult;
import org.junit.Test;

import java.util.List;

public class MetaWrapperResourceTest {
    @Test
    public void testRequest() throws Exception {

        //MetaWrapperResource resource = new MetaWrapperResource();
        //List<MMResult> results = resource.getResults("cancer");
        //System.out.println(results.get(0).getUtterances().get(0).getPcmList().get(0).getCandidates().get(2).getConceptId());
    }
}
